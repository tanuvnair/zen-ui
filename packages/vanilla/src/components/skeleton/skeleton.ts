import { styled } from "../../lib/styled";

/** Skeleton — a pulsing placeholder box. zen-animate-pulse is a real Uno utility. */
export const Skeleton = styled({ tag: "div", className: "zen-animate-pulse zen-rounded-zen-md zen-bg-zen-muted" });
export type { BaseProps as SkeletonProps } from "../../lib/component";
