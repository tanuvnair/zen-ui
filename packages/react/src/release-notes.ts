/**
 * The release notes live in @algorisys/zen-ui-core/release-notes — one copy for one
 * design system. This file stays so the demo's existing imports do not move, and so
 * a binding could still add a note of its own if it ever genuinely had one.
 */
export * from "@algorisys/zen-ui-core/release-notes";
