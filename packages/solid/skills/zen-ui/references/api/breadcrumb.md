<!-- GENERATED FILE — do not edit by hand.
     Source: packages/react/src types (via scripts/gen-skill-api.ts)
     Regenerate: bun run gen:skill-api  (checked by `bun run check`) -->

# breadcrumb — API (React, the parity reference)

Exports: `Breadcrumb`, `BreadcrumbList`, `BreadcrumbItem`, `BreadcrumbLink`, `BreadcrumbPage`, `BreadcrumbSeparator`, `BreadcrumbEllipsis`

Solid mirrors these props. Vanilla takes the same props as a factory argument
(handle out, `.el` is the node); web-components as `<zen-breadcrumb>`-style
attributes/properties. Divergences (Select, Toast, data-driven families) are in
SKILL.md.

### Breadcrumb

- `separator?: React.ReactNode`
- …plus the underlying element's standard props (280 inherited).

### BreadcrumbList

- …plus the underlying element's standard props (283 inherited).

### BreadcrumbItem

- …plus the underlying element's standard props (281 inherited).

### BreadcrumbLink

- `asChild?: boolean | undefined`
- …plus the underlying element's standard props (288 inherited).

### BreadcrumbPage

- …plus the underlying element's standard props (280 inherited).

### BreadcrumbSeparator

- …plus the underlying element's standard props (281 inherited).

### BreadcrumbEllipsis

- …plus the underlying element's standard props (280 inherited).
